import React from 'react'
import { FormControlLabel } from '@mui/material'

const RadioCheckbox = ({ ...props }) => (
  <FormControlLabel {...props} />
)

export default RadioCheckbox // Find better name
